<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        
        <!-- Categories -->
        <record id="module_category_tracking" model="ir.module.category">
            <field name="name">Tracking</field>
            <field name="sequence">20</field>
        </record>

        <!-- User Group -->
        <record id="group_tracking_user" model="res.groups">
            <field name="name">User</field>
            <field name="category_id" ref="module_category_tracking"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <!-- Manager Group -->
        <record id="group_tracking_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_tracking"/>
            <field name="implied_ids" eval="[(4, ref('group_tracking_user'))]"/>
        </record>

    </data>
</odoo>
